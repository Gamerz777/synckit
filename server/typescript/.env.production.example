# SyncKit Server - Production Environment Configuration
# Copy this file to .env.production and fill in real values

# Server Configuration
NODE_ENV=production
HOST=0.0.0.0
PORT=8080

# Database (PostgreSQL)
# Example: postgresql://username:password@hostname:5432/database
DATABASE_URL=postgresql://synckit:CHANGE_THIS_PASSWORD@postgres:5432/synckit
DB_POOL_MIN=2
DB_POOL_MAX=10

# Redis (Cache & Pub/Sub)
# Example: redis://hostname:6379 or rediss://hostname:6379 (with TLS)
REDIS_URL=redis://redis:6379
REDIS_CHANNEL_PREFIX=synckit:

# JWT Authentication
# CRITICAL: Generate a secure secret (min 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_32_CHARS
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# WebSocket Configuration
WS_HEARTBEAT_INTERVAL=30000
WS_HEARTBEAT_TIMEOUT=60000
WS_MAX_CONNECTIONS=10000

# Sync Configuration
SYNC_BATCH_SIZE=100
SYNC_BATCH_DELAY=50

# Optional: Monitoring & Logging
# SENTRY_DSN=https://your-sentry-dsn
# LOG_LEVEL=info
